# This empty file marks the "app" folder as a Python *package*.
# Node.js analogy: it's roughly what having a package.json + index.js does for a folder —
# it lets other code do `from app.main import app` (like `require('./app')`).
